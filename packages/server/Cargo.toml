[package]
name = "hermes-server"
version = "0.1.0"
edition = "2024"

[dependencies]
oxide-auth = "0.6.1"
rocket = { version = "0.5.1" }
rocket_ws = { version = "0.1.1" }
sqlx = { version = "0.8", features = [ "runtime-tokio", "sqlite", "macros","migrate","bstr", "time"] }